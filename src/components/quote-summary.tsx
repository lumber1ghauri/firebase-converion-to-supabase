// This component is no longer used and can be removed.
// The quote summary is now part of the quote-confirmation page.
// Keeping the file to avoid breaking imports if it's referenced elsewhere,
// but it should be considered for deletion.

export function QuoteSummary() {
  return null;
}
